from django.contrib import admin

# Django's built-in User model is already registered by
# django.contrib.auth — nothing extra needed here.
# Visit /admin/ to manage users via the Django admin interface.
