# We use Django's built-in User model directly — no extension needed.
#
# The built-in User provides:
#   id, username, email, password (hashed), date_joined, is_active, …
#
# Import it wherever you need it:
#   from django.contrib.auth.models import User
