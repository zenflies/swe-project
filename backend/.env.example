# Django
SECRET_KEY=your-secret-key-here-change-this-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL Database
DB_NAME=itinerate_db
DB_USER=postgres
DB_PASSWORD=amaan786
DB_HOST=localhost
DB_PORT=5432

# CORS — comma-separated list of allowed frontend origins
# Add your frontend's origin here (e.g. Live Server, Vite dev server, etc.)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5500,http://127.0.0.1:5500,http://localhost:8080